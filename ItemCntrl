package example_mvc.controller;

import example_mvc.model.Item;
import example_mvc.model.ItemList;

public class ItemCntrl {
    private Item item;
    private ItemList  itemList;
    private ItemUI itemUI;
    public ItemCntrl() {
        itemList = new ItemList();
        itemUI = new ItemUI (this);
        itemUI.setVisible(true);
    }
}
